--- ruff_unsafe_golden_input.py
+++ ruff_unsafe_golden_input.py
@@ -5,6 +5,8 @@
 """
 
 
-def add_item(item: int, items: list[int] = []) -> list[int]:
+def add_item(item: int, items: list[int] = None) -> list[int]:
+    if items is None:
+        items = []
     items.append(item)
     return items

Would fix 1 error.
