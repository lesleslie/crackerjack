[build-system]
requires = [
    "hatchling",
]
build-backend = "hatchling.build"

[project]
name = "crackerjack"
version = "0.44.25"
description = "Crackerjack Python project management tool"
readme = "README.md"
authors = [
    { name = "Les Leslie", email = "les@wedgwoodwebworks.com" },
]
requires-python = ">=3.13"
classifiers = [
    "Operating System :: POSIX",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Software Development :: Quality Assurance",
    "Topic :: Software Development :: Testing",
    "Topic :: Utilities",
    "Typing :: Typed",
]
dependencies = [
    "aiofiles>=25.1.0",
    "aiohttp>=3.13.2",
    "codespell>=2.4.1",
    "complexipy>=4.2.0",
    "creosote>=4.1.0",
    "fastapi>=0.121.2",
    "fastmcp>=2.13.1",
    "hatchling>=1.27.0",
    "jinja2>=3.1.6",
    "keyring>=25.6.0",
    "mcp>=1.21.0",
    "nltk>=3.9.2",
    "numpy>=2.3.4",
    "pydantic>=2.12.4",
    "pyleak>=0.1.14",
    "pyright>=1.1.407",
    "pytest>=9.0.1",
    "pytest-asyncio>=1.3.0",
    "pytest-benchmark>=5.2.3",
    "pytest-cov>=7.0.0",
    "pytest-mock>=3.15.1",
    "pytest-timeout>=2.4.0",
    "pytest-xdist>=3.8.0",
    "hypothesis>=6.148.1",
    "psutil>=7.1.3",
    "pyyaml>=6.0.3",
    "refurb>=2.2.0",
    "rich>=14.2.0",
    "scikit-learn>=1.7.2",
    "scipy>=1.16.3",
    "ruff>=0.14.5",
    "onnxruntime>=1.23.2",
    "transformers>=4.57.1",
    "structlog>=25.5.0",
    "textual>=6.6.0",
    "tomli-w>=1.2.0",
    "typer>=0.20.0",
    "uv>=0.9.9",
    "uvicorn>=0.38.0",
    "vulture>=2.14",
    "watchdog>=6.0.0",
    "websockets>=15.0.1",
    "skylos>=2.5.0",
    "zuban>=0.2.2",
    "types-pyyaml>=6.0.12.20250915",
    "types-psutil>=7.0.0.20251116",
    "types-aiofiles>=25.1.0.20251011",
    "scipy-stubs>=1.16.3.0",
    "session-mgmt-mcp>=0.9.2",
    "mcp-common>=0.3.2",
    "acb>=0.31.4",
    "mdformat>=1.0.0",
    "mdformat-ruff>=0.1.3",
    "bevy>=3.1.0b12",
    "uv-bump>=0.3.1",
]

[dependency-groups]
# SAST (Static Application Security Testing) tools
# Install with: uv sync --group sast
sast = [
    "bandit>=1.8.6",  # Python security linter
#     "semgrep>=1.95",  # Disabled: incompatible with rich>=14.1 (requires rich<13.6)
]

# SCA (Software Composition Analysis) - Dependency vulnerability scanning
# Install with: uv sync --group dependency
dependency = [
    "pip-audit>=2.9.0",  # Dependency vulnerability scanner (CVE detection)
]

# Development dependencies
# Install with: uv sync --group dev
dev = [
    "excalidraw-mcp>=0.34.0",
    "session-mgmt-mcp>=0.9.2",
    "ty>=0.0.1a26",  # Experimental type checker (Astral) - monitoring only, not for production use
]

[project.license]
text = "BSD-3-CLAUSE"

[project.scripts]
crackerjack = "crackerjack.__main__:cli"

[project.urls]
documentation = "https://github.com/lesleslie/crackerjack"
homepage = "https://github.com/lesleslie/crackerjack"
repository = "https://github.com/lesleslie/crackerjack"

[tool.hatch.build.targets.wheel]
include = [
    "crackerjack/**/*.py",
    "crackerjack/**/*.md",
]

[tool.ruff]
target-version = "py313"
line-length = 88
fix = true
unsafe-fixes = true
show-fixes = true
output-format = "full"
exclude = [
    "tests/",
    "test_*.py",
    "*_test.py",
]

[tool.ruff.format]
docstring-code-format = true

[tool.ruff.lint]
extend-select = [
    "C901",
    "F",
    "I",
    "UP",
]
ignore = [
    "E402",
    "F821",
]
fixable = [
    "ALL",
]

[tool.ruff.lint.mccabe]
max-complexity = 15

[tool.crackerjack]
mcp_http_port = 8676
mcp_http_host = "127.0.0.1"
mcp_websocket_port = 8675
mcp_http_enabled = true
zuban_lsp_enabled = false
zuban_lsp_host = "127.0.0.1"
zuban_lsp_port = 8677
zuban_lsp_timeout = 10.0
zuban_lsp_strict_mode = true
terminal_width = 70

# Test parallelization settings
test_workers = 0  # 0 = auto-detect, 1 = sequential, >1 = explicit, <0 = fractional
max_workers = 8  # Maximum parallel workers (safety limit)
memory_per_worker_gb = 2.0  # Minimum memory per worker (prevents OOM)

[tool.codespell]
# Note: Native wrapper (crackerjack.tools.codespell_wrapper) uses git ls-files
# to discover files, automatically respecting .gitignore patterns.
# No skip patterns needed.
quiet-level = 3
ignore-words-list = "crate,uptodate,nd,nin"
ignore-words = ".codespell-ignore"

[tool.pytest.ini_options]
asyncio_mode = "auto"
timeout = 300
addopts = "--cov=crackerjack --cov-report=term-missing:skip-covered --cov-report=html:htmlcov --cov-report=json --strict-markers --strict-config -ra --tb=short"
testpaths = [
    "tests",
]
norecursedirs = [
    "~",
    ".git",
    "*.egg",
    "build",
    "dist",
    ".venv",
    "__pycache__",
    "node_modules",
]
markers = [
    "unit: marks test as a unit test",
    "benchmark: mark test as a benchmark",
    "integration: marks test as an integration test",
    "e2e: marks test as end-to-end test",
    "security: marks test as security test",
    "performance: marks test as performance test",
    "slow: marks test as slow running test",
    "smoke: marks test as smoke test",
    "regression: marks test as regression test",
    "api: marks test as API test",
    "database: marks test as database test",
    "external: marks test requiring external services",
    "no_leaks: detect asyncio task leaks",
    "property: marks test as property-based test",
    "mutation: marks test as mutation testing",
    "chaos: marks test as chaos engineering test",
    "ai_generated: marks test as AI-generated test",
    "breakthrough: marks test as breakthrough frontier test",
]
# pytest-benchmark configuration (CLI options, not ini_options)
# Benchmark settings are passed via command line or kept in separate [tool.pytest.benchmark]
# Removed to avoid "Unknown config option" warnings

[tool.coverage.run]
branch = true  # Enable branch coverage for more accurate metrics
source = [
    "crackerjack",
]
data_file = ".coverage"
parallel = true
concurrency = ["multiprocessing"]
omit = [
    "*/tests/*",
    "*/site-packages/*",
    "*/__pycache__/*",
    "*/__init__.py",
    "*/_version.py",
    "*/conftest.py",
    "*/test_*.py",
    "*/_test.py",
    "crackerjack/__main__.py",
]

[tool.coverage.report]
exclude_also = [
    "pragma: no cover",
    "def __repr__",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "pass",
    "raise ImportError",
    "except ImportError",
    "def __str__",
    "@abstractmethod",
]
ignore_errors = false

[tool.pyright]
verboseOutput = true
include = [
    "crackerjack",
]
exclude = [
    "scratch",
    ".venv",
    "*/.venv",
    "**/.venv",
    "build",
    "dist",
    "tests/*",
    "examples/*",
    "crackerjack/mcp/*",
    "crackerjack/plugins/*",
]
extraPaths = [
    ".venv/lib/python3.13/site-packages/",
]
typeCheckingMode = "strict"
reportMissingTypeStubs = false
reportOptionalMemberAccess = false
reportOptionalCall = "warning"
reportUnknownMemberType = false
reportUnknownVariableType = false
reportUnknownArgumentType = false
reportInvalidTypeForm = "warning"
reportUnknownLambdaType = false
reportUnknownParameterType = false
reportPrivateUsage = false
reportUnnecessaryTypeIgnoreComment = "warning"
reportUnnecessaryComparison = "warning"
reportConstantRedefinition = "warning"
pythonVersion = "3.13"

[tool.uv]
keyring-provider = "subprocess"
publish-url = "https://upload.pypi.org/legacy/"
check-url = "https://pypi.org/simple/"


# [tool.uv.sources.acb]
# path = "../acb"
# editable = true

[tool.creosote]
paths = [
    "crackerjack",
]
deps-file = "pyproject.toml"
exclude-deps = [
    "hatchling",
    "pre-commit",
    "pytest",
    "pytest-asyncio",
    "pytest-cov",
    "pytest-mock",
    "pytest-xdist",
    "pytest-benchmark",
    "pyfiglet",
    "pyyaml",
    "uv",
    "tomli-w",
    "google-crc32c",
    "pytest-timeout",
    "keyring",
    "inflection",
    "pydantic-settings",
    "pyleak",
    "bandit",
    "vulture",
    "pyright",
    "creosote",
    "refurb",
    "complexipy",
    "codespell",
    "fastapi",
    "fastmcp",
    "uvicorn",
    "websockets",
    "watchdog",
    "ruff",
    "hypothesis",
    "psutil",
    "skylos",
    "zuban",
    "types-pyyaml",
    "types-psutil",
    "types-aiofiles",
    "onnxruntime",
    "transformers",
    "scikit-learn",
    "numpy",
    "nltk",
    "scipy-stubs",
    "session-mgmt-mcp",
    "structlog",
    "mdformat",
    "mdformat-ruff",
    "bevy",
]

[tool.refurb]
enable_all = true
quiet = true
python_version = "3.13"
ignore = [
    "FURB184",  # Already applies to ALL files including tests
    "FURB120",  # Already applies to ALL files including tests
]

[tool.bandit]
target = [
    "crackerjack",
]
skips = [
    "B101",
    "B110",
    "B112",
    "B404",
    "B603",
    "B607",
]
exclude_dirs = [
    "tests/",
    "test_*.py",
    "*_test.py",
]

[tool.complexipy]
default_pattern = "**/*.py"
exclude_patterns = [
    "**/tests/**",
    "**/test_*.py",
]
max_complexity = 15

[tool.mypy]
# NOTE: Full mypy config in mypy.ini (zuban limitation)
# Zuban v0.2.2 has a bug parsing [tool.mypy] from pyproject.toml
# Error: "Expected tool.mypy to be simple table in pyproject.toml"
# Once zuban is fixed, consolidate mypy.ini contents here
# See: UNIFIED_IMPLEMENTATION_PLAN.md Phase 2 Task 2.1 (BLOCKED)
python_version = "3.13"

[tool.zuban]
strict = true
show_error_codes = true



[tool.zuban.lsp]
host = "127.0.0.1"
port = 8677
timeout = 10.0
enable_diagnostics = true
real_time_feedback = true
